'use client';
import {createTheme} from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F44D03',
    },
    secondary: {
      main: '#F44D03',
    },
  },
});

export {theme};
